{{!
    @template continue_session

    Example context (json):
    {
        "str": "Continue session...",
        "path": "https://example.com/"
    }
}}
<div class="jazzquiz-box">
    <p>{{# str }} instructor_sessions_going, jazzquiz {{/ str }}</p>
    <p>
        <a href="{{ path }}" class="btn btn-secondary">{{# str }} goto_session, jazzquiz {{/ str }}</a>
    </p>
</div>